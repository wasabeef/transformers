<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="jp.wasabeef.transformers"
  >

  <uses-permission android:name="android.permission.INTERNET" />

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    >
    <activity
      android:name="jp.wasabeef.transformers.MainActivity"
      android:label="@string/app_name"
      >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name="jp.wasabeef.transformers.GlideActivity" />
    <activity android:name="jp.wasabeef.transformers.PicassoActivity" />
    <activity android:name="jp.wasabeef.transformers.CoilActivity" />
    <activity android:name="jp.wasabeef.transformers.FrescoActivity" />
  </application>

</manifest>